Git = track changes in our code and let's us easily undo our changes
Repository = a folder where changes are being tracked

data attribute = is just an HTML attribute, we can attach any info to an element
syntax-  data-product-name = '${product-name}'
- it have to start with "data-" then give it any names
-this is also known as kebab-case

modules - a better way to organize our code
-avoids naming conflict
- any variables we create inside the file, will be contained inside the file

get a variable out of a file-
    1.add type= 'module' attribute
    2. export
    3. import

put all imports at the top of the file
we need to use live server

benefits: 
    -helps us avoid naming conflicts
    -don't have to worry about order of files
    modules= better way to organize our code
